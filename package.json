{
  "name": "calendar-statistics-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "install": "npm run install:all",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "start": "concurrently -k -n backend,frontend -c green,blue \"npm --prefix backend run dev\" \"npm --prefix frontend run start\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}

